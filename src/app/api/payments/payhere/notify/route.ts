// Canonical PayHere callback URL. The legacy path reuses the same verified
// handler so existing sandbox configuration remains safe during rollout.
export { POST } from "@/app/api/payhere/notify/route";
