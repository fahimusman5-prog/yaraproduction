"use client";
export function AdminError({ reset }: { reset: () => void }) { return <div className="staff-panel mx-auto max-w-lg p-8 text-center"><h1 className="text-xl font-bold">We couldn’t load this admin page</h1><p className="mt-2 text-sm text-slate-500">Check the Supabase connection and try again.</p><button onClick={reset} className="staff-button staff-button-primary mt-5">Try again</button></div>; }
